// src/ui/theme/index.js

import { createMuiTheme } from '@material-ui/core/styles';

const palette = {
  primary: { main: '#BCAAA4' },
  secondary: { main: '#A5D6A7' }
};
const themeName = 'Silk Moss Green Baby Doll Sheep';

export default createMuiTheme({ palette, themeName });